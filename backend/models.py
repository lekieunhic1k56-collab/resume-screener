from sqlalchemy import Column, Integer, String, ForeignKey\nfrom sqlalchemy.orm import relationship\n\n# Mô hình JobDescription đại diện cho bảng mô tả công việc\nclass JobDescription(Base):\n    __tablename__ = 'job_descriptions'\n    \n    # ID công việc (khóa chính)\n    id = Column(Integer, primary_key=True, autoincrement=True)\n    # Tiêu đề công việc\n    title = Column(String(255), nullable=False)\n    # Mô tả công việc chi tiết\n    description = Column(String, nullable=False)\n    \n    # Quan hệ với bảng CV\n    cvs = relationship('CV', back_populates='job_description')\n\n# Mô hình CV đại diện cho bảng hồ sơ cá nhân\nclass CV(Base):\n    __tablename__ = 'cvs'\n    \n    # ID hồ sơ (khóa chính)\n    id = Column(Integer, primary_key=True, autoincrement=True)\n    # Tên của ứng viên\n    candidate_name = Column(String(255), nullable=False)\n    # Liên kết đến mô tả công việc\n    job_description_id = Column(Integer, ForeignKey('job_descriptions.id'))\n    # Quan hệ với bảng JobDescription\n    job_description = relationship('JobDescription', back_populates='cvs')\n    \n    # Các tiêu chí đánh giá liên quan\n    scoring_criteria = relationship('ScoringCriteria', back_populates='cv')\n\n# Mô hình ScoringCriteria đại diện cho bảng tiêu chí đánh giá\nclass ScoringCriteria(Base):\n    __tablename__ = 'scoring_criteria'\n    \n    # ID tiêu chí đánh giá (khóa chính)\n    id = Column(Integer, primary_key=True, autoincrement=True)\n    # Nội dung tiêu chí\n    criterion = Column(String(255), nullable=False)\n    # Điểm số cho tiêu chí đó\n    score = Column(Integer, nullable=False)\n    # Liên kết đến hồ sơ ứng viên\n    cv_id = Column(Integer, ForeignKey('cvs.id'))\n    # Quan hệ với bảng CV\n    cv = relationship('CV', back_populates='scoring_criteria')